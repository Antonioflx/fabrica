// Não quero ter o reconhecimento disso.
// Esqueci que no objeto poderia utilizar uma função -> lembrava, mas esqueci total. Perguntei, e ele indicou essa.
// essa parte foi o chat GPT. -> Irei utilizar nos meus futuros projetos.

// todo o resto do projeto foi meu.

// Repito Não quero ter reconhecimento desse arquivo aqui.
const operation = {
	"+": (num1, num2) => num1 + num2,
	"-": (num1, num2) => num1 - num2,
	"*": (num1, num2) => num1 * num2,
	"/": (num1, num2) => (num2 !== 0 ? num1 / num2 : "Error "),
};

export default operation;
